#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void) {

	int A=-1, B=-1;
	scanf("%d %d", &A, &B);

	while (A != 0 && B != 0) {

		printf("%d\n", A + B);
		scanf("%d %d", &A, &B);

	}

	return 0;
}
