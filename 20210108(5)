#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void) {

	int A = -1, B = -1, z;
	z = scanf("%d %d", &A, &B);

	while (z != EOF) {
		printf("%d\n", A + B);
		z = scanf("%d %d", &A, &B);
	}

	return 0;
}
